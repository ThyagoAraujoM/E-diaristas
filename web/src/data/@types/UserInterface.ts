// Utilizando typescript para criar uma lista das propriedades que a diarista precisa ter que facilitam na hora de declarar
export interface UserShortInterface {
   nome_completo: string;
   foto_usuario: string;
   reputação: number;
   cidade: string;
}
